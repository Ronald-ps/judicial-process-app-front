export const PageDefaultMarginsContainer = ({ children }) => {
  return (
    <div
      style={{
        paddingTop: "24px",
        paddingLeft: "56px",
        paddingBottom: "48px",
        paddingRight: "32px",
      }}
    >
      {children}
    </div>
  );
};
