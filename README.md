## Setup
```shell
# Copia arquivo contendo variáveis de ambiente
$ cp .env.dev .env
# Roda api mock
$ node src/mock/api-mock/server.js
```

### Api Mock
```shell
$ node src/mock/api-mock/server.js
```
